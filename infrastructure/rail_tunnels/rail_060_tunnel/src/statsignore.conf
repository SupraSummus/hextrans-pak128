This file tells copyright/stats processing scripts not to look here.

Legacy upstream pak128 art for rail_060_tunnel, kept as supervision
input for the model in the parent dir but no longer packaged
(makeobj is non-recursive and only scans `infrastructure/rail_tunnels/`).

Only this file name is important.
