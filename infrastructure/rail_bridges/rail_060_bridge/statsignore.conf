This file tells copyright/stats processing scripts not to look here.

This directory holds the 3D model and supervision artefacts for
rail_060_bridge (the upstream pakset asset is rail_060_bridge.{png,dat}
in the parent directory). It contains no pakset assets.

Only this file name is important.
